import { Figtree, Sono } from 'next/font/google'

export const figtree = Figtree({
  weight: ['400', '500'],
  subsets: ['latin'],
  variable: '--font-fig',
})
export const sono = Sono({
  weight: ['400', '500'],
  subsets: ['latin'],
  variable: '--font-sono',
})
